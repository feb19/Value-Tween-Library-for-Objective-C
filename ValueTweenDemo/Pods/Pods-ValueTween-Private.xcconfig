#include "Pods-ValueTween.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/ValueTween" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/ValueTween"
OTHER_LDFLAGS = -ObjC ${PODS_VALUETWEEN_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}